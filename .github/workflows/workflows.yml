name: CI/CD Pipeline

on:
  push:
    branches:
      - selenium-framework

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run tests
        run: |
          mvn test
