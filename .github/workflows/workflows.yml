name: CI/CD Pipeline Test devdomain

on:
  push:
    branches:
      - selenium-framework

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run tests
        run: |
          mvn clean test
